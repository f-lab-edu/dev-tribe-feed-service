spring:
  datasource:
    username: root
    password: test
    url: jdbc:tc:mysql:8.0://devtribe-feed?useUnicode=yes&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=utf8?allowPublicKeyRetrieval=true
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database: mysql
    properties:
      hibernate:
        show_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.transaction: TRACE
    org.springframework.transaction: TRACE
